# 图书管理系统 — 测试大纲

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 图书管理系统 |
| 文档版本 | 1.0 |
| 编制日期 | 2026-07-04 |
| 技术栈 | Vue3 + Element-Plus / NodeJS + Express / SQLite |

---

## 优先级说明

| 优先级 | 含义 |
|--------|------|
| P0 | 核心功能，阻塞发布 |
| P1 | 重要功能，必须在首个版本通过 |
| P2 | 辅助功能，可后续版本覆盖 |

---

## 1. 用户管理模块

### 1.1 用户注册

| 用例编号 | TC-UM-001 |
|----------|-----------|
| 测试点 | 正常注册：使用合法用户名、密码、邮箱完成注册 |
| 前置条件 | 数据库无同名用户，前端注册页面可访问 |
| 预期结果 | 用户创建成功，数据库新增一条用户记录，密码加密存储，前端提示注册成功并跳转登录页 |
| 优先级 | P0 |

| 用例编号 | TC-UM-002 |
|----------|-----------|
| 测试点 | 重复注册：使用已存在的用户名注册 |
| 前置条件 | 目标用户名已存在于数据库 |
| 预期结果 | 注册失败，前端提示"用户名已被注册"，数据库无新增记录 |
| 优先级 | P0 |

| 用例编号 | TC-UM-003 |
|----------|-----------|
| 测试点 | 必填字段为空：用户名为空、密码为空、邮箱为空三种场景各自提交 |
| 前置条件 | 注册页面可访问 |
| 预期结果 | 前端表单校验拦截，提示对应字段必填，请求不发送至后端 |
| 优先级 | P1 |

| 用例编号 | TC-UM-004 |
|----------|-----------|
| 测试点 | 弱密码注册：密码长度 < 6 位或纯数字 |
| 前置条件 | 注册页面可访问 |
| 预期结果 | 前端校验提示"密码长度至少6位且需包含字母"，或后端返回校验失败 |
| 优先级 | P1 |

| 用例编号 | TC-UM-005 |
|----------|-----------|
| 测试点 | 邮箱格式非法（如 `abc`、`abc@`、`@abc.com`） |
| 前置条件 | 注册页面可访问 |
| 预期结果 | 前端邮箱校验拦截，提示"请输入正确的邮箱格式" |
| 优先级 | P2 |

### 1.2 用户登录

| 用例编号 | TC-UM-006 |
|----------|-----------|
| 测试点 | 正常登录：已注册用户使用正确用户名和密码登录 |
| 前置条件 | 用户已注册且状态正常 |
| 预期结果 | 登录成功，后端返回 token，前端跳转至系统首页，token 写入本地存储 |
| 优先级 | P0 |

| 用例编号 | TC-UM-007 |
|----------|-----------|
| 测试点 | 密码错误：使用错误的密码登录 |
| 前置条件 | 用户名存在 |
| 预期结果 | 登录失败，提示"用户名或密码错误"，不返回 token |
| 优先级 | P0 |

| 用例编号 | TC-UM-008 |
|----------|-----------|
| 测试点 | 用户不存在：使用不存在的用户名登录 |
| 前置条件 | 数据库无该用户名 |
| 预期结果 | 登录失败，提示"用户名或密码错误"（不暴露用户是否存在） |
| 优先级 | P1 |

| 用例编号 | TC-UM-009 |
|----------|-----------|
| 测试点 | Token 过期后访问受保护接口 |
| 前置条件 | 用户已登录但 token 已过期 |
| 预期结果 | 后端返回 401，前端拦截并跳转至登录页 |
| 优先级 | P1 |

### 1.3 权限控制

| 用例编号 | TC-UM-010 |
|----------|-----------|
| 测试点 | 系统管理员访问全部管理功能 |
| 前置条件 | 以系统管理员角色登录 |
| 预期结果 | 可访问用户管理、系统配置、图书管理、借阅管理、统计等全部功能模块 |
| 优先级 | P0 |

| 用例编号 | TC-UM-011 |
|----------|-----------|
| 测试点 | 普通读者尝试访问管理后台 |
| 前置条件 | 以普通读者角色登录 |
| 预期结果 | 前端路由守卫拦截管理后台路由，提示无权限或跳转至读者首页 |
| 优先级 | P0 |

| 用例编号 | TC-UM-012 |
|----------|-----------|
| 测试点 | 图书管理员访问核心管理功能 |
| 前置条件 | 以图书管理员角色登录 |
| 预期结果 | 可访问图书管理、借阅管理模块，不可访问系统配置模块 |
| 优先级 | P1 |

| 用例编号 | TC-UM-013 |
|----------|-----------|
| 测试点 | 未登录直接访问受保护路由 |
| 前置条件 | 未登录状态 |
| 预期结果 | 前端路由守卫拦截，跳转至登录页 |
| 优先级 | P1 |

### 1.4 用户信息维护

| 用例编号 | TC-UM-014 |
|----------|-----------|
| 测试点 | 修改个人信息（昵称、邮箱、手机号） |
| 前置条件 | 用户已登录 |
| 预期结果 | 修改成功，数据库更新对应字段，前端提示"信息更新成功" |
| 优先级 | P1 |

| 用例编号 | TC-UM-015 |
|----------|-----------|
| 测试点 | 修改密码：输入旧密码 + 新密码 |
| 前置条件 | 用户已登录 |
| 预期结果 | 旧密码校验通过后新密码生效，旧密码无法再登录 |
| 优先级 | P1 |

| 用例编号 | TC-UM-016 |
|----------|-----------|
| 测试点 | 修改密码时旧密码错误 |
| 前置条件 | 用户已登录 |
| 预期结果 | 修改失败，提示"旧密码错误"，密码不被更改 |
| 优先级 | P1 |

---

## 2. 图书管理模块

### 2.1 图书入库

| 用例编号 | TC-BM-001 |
|----------|-----------|
| 测试点 | 正常入库：填入合法的 ISBN、书名、作者、出版社、分类、数量等信息 |
| 前置条件 | 以图书管理员身份登录，ISBN 未在库中 |
| 预期结果 | 图书入库成功，数据库新增图书记录，库存数量正确 |
| 优先级 | P0 |

| 用例编号 | TC-BM-002 |
|----------|-----------|
| 测试点 | 重复 ISBN 入库：对已存在的 ISBN 执行入库 |
| 前置条件 | 目标 ISBN 已有图书记录 |
| 预期结果 | 入库失败或被引导至"增加库存"操作，提示 ISBN 已存在 |
| 优先级 | P1 |

| 用例编号 | TC-BM-003 |
|----------|-----------|
| 测试点 | 入库必填字段为空（ISBN / 书名为空） |
| 前置条件 | 入库页面可访问 |
| 预期结果 | 前端校验拦截，提示必填字段，不提交请求 |
| 优先级 | P1 |

| 用例编号 | TC-BM-004 |
|----------|-----------|
| 测试点 | 批量入库：一次提交多条图书记录 |
| 前置条件 | 以图书管理员身份登录 |
| 预期结果 | 所有合法记录入库成功，数据完整性正确；如有非法记录仅该条失败，其余正常入库 |
| 优先级 | P2 |

### 2.2 图书编目

| 用例编号 | TC-BM-005 |
|----------|-----------|
| 测试点 | 正常编目：为已有图书补充编目信息（分类号、索书号、馆藏位置） |
| 前置条件 | 图书记录已存在，编目信息缺失 |
| 预期结果 | 编目信息更新成功，图书可按分类号和索书号检索 |
| 优先级 | P0 |

| 用例编号 | TC-BM-006 |
|----------|-----------|
| 测试点 | 编辑已有编目信息（修改分类） |
| 前置条件 | 图书记录已有编目信息 |
| 预期结果 | 编目信息更新成功，前端提示更新成功 |
| 优先级 | P2 |

### 2.3 图书查询

| 用例编号 | TC-BM-007 |
|----------|-----------|
| 测试点 | 按书名精确查询 |
| 前置条件 | 数据库存在目标图书 |
| 预期结果 | 返回且仅返回匹配的图书记录 |
| 优先级 | P0 |

| 用例编号 | TC-BM-008 |
|----------|-----------|
| 测试点 | 按作者模糊查询 |
| 前置条件 | 数据库存在该作者的多本图书 |
| 预期结果 | 返回该作者所有图书列表 |
| 优先级 | P0 |

| 用例编号 | TC-BM-009 |
|----------|-----------|
| 测试点 | 按分类检索 |
| 前置条件 | 数据库存在该分类的图书 |
| 预期结果 | 返回该分类下所有在库图书 |
| 优先级 | P1 |

| 用例编号 | TC-BM-010 |
|----------|-----------|
| 测试点 | 组合条件查询（书名 + 作者 + 分类） |
| 前置条件 | 数据库存在匹配全部条件的图书 |
| 预期结果 | 返回同时满足所有条件的图书记录 |
| 优先级 | P1 |

| 用例编号 | TC-BM-011 |
|----------|-----------|
| 测试点 | 查询不存在的图书（书名无匹配） |
| 前置条件 | 数据库无匹配记录 |
| 预期结果 | 返回空列表，前端展示"未找到相关图书" |
| 优先级 | P2 |

### 2.4 图书下架

| 用例编号 | TC-BM-012 |
|----------|-----------|
| 测试点 | 正常下架：对未被借出的图书执行下架 |
| 前置条件 | 图书在库且未被借出 |
| 预期结果 | 图书状态标记为"已下架"，不再出现在检索结果中 |
| 优先级 | P0 |

| 用例编号 | TC-BM-013 |
|----------|-----------|
| 测试点 | 下架已被借出的图书 |
| 前置条件 | 图书当前处于"已借出"状态 |
| 预期结果 | 下架失败，提示"该图书当前已被借出，无法下架" |
| 优先级 | P1 |

| 用例编号 | TC-BM-014 |
|----------|-----------|
| 测试点 | 重新上架已下架的图书 |
| 前置条件 | 图书处于"已下架"状态 |
| 预期结果 | 图书状态恢复为"在库"，重新出现在检索结果中 |
| 优先级 | P2 |

---

## 3. 借阅管理模块

### 3.1 借书

| 用例编号 | TC-BR-001 |
|----------|-----------|
| 测试点 | 正常借书：读者在借阅上限内借阅在库图书 |
| 前置条件 | 读者已登录，已借数量 < 上限（默认 5 本），图书状态为"在库" |
| 预期结果 | 借阅成功，图书记录状态变为"已借出"，读者借阅列表新增一条记录，归还日期为借出日 +30 天 |
| 优先级 | P0 |

| 用例编号 | TC-BR-002 |
|----------|-----------|
| 测试点 | 超限借书：读者已借数量达到上限后再借书 |
| 前置条件 | 读者当前借阅数量 = 5（达到上限） |
| 预期结果 | 借阅失败，提示"借阅数量已达上限，请先归还后再借" |
| 优先级 | P0 |

| 用例编号 | TC-BR-003 |
|----------|-----------|
| 测试点 | 借阅已借出的图书 |
| 前置条件 | 目标图书状态为"已借出" |
| 预期结果 | 借阅失败，提示"该图书已被借出" |
| 优先级 | P0 |

| 用例编号 | TC-BR-004 |
|----------|-----------|
| 测试点 | 借阅已下架图书 |
| 前置条件 | 目标图书状态为"已下架" |
| 预期结果 | 借阅失败，提示"该图书已下架，不可借阅" |
| 优先级 | P1 |

### 3.2 还书

| 用例编号 | TC-BR-005 |
|----------|-----------|
| 测试点 | 正常还书：归还一本读者已借出的在借图书 |
| 前置条件 | 读者持有该图书的借阅记录 |
| 预期结果 | 还书成功，图书状态恢复为"在库"，借阅记录标记为"已归还"，读者借阅数量 -1 |
| 优先级 | P0 |

| 用例编号 | TC-BR-006 |
|----------|-----------|
| 测试点 | 归还非本人借出的图书 |
| 前置条件 | 读者 A 尝试归还读者 B 借出的图书 |
| 预期结果 | 还书失败，提示"未找到该借阅记录" |
| 优先级 | P1 |

### 3.3 续借

| 用例编号 | TC-BR-007 |
|----------|-----------|
| 测试点 | 正常续借：在应还日期前且未达续借次数上限发起续借 |
| 前置条件 | 读者持有有效的借阅记录，续借次数 < 上限（默认 1 次），未逾期 |
| 预期结果 | 续借成功，应还日期延长 30 天，续借次数 +1 |
| 优先级 | P0 |

| 用例编号 | TC-BR-008 |
|----------|-----------|
| 测试点 | 续借已达上限（已续借 1 次后再续借） |
| 前置条件 | 该借阅记录续借次数 = 1（已用尽） |
| 预期结果 | 续借失败，提示"续借次数已用完" |
| 优先级 | P1 |

| 用例编号 | TC-BR-009 |
|----------|-----------|
| 测试点 | 逾期后尝试续借 |
| 前置条件 | 借阅记录已逾期（当前日期 > 应还日期） |
| 预期结果 | 续借失败，提示"逾期图书不可续借，请先归还" |
| 优先级 | P1 |

| 用例编号 | TC-BR-010 |
|----------|-----------|
| 测试点 | 有他人预约时尝试续借 |
| 前置条件 | 该图书已被其他读者预约 |
| 预期结果 | 续借失败或提示"该图书已被预约，不可续借" |
| 优先级 | P2 |

### 3.4 逾期处理

| 用例编号 | TC-BR-011 |
|----------|-----------|
| 测试点 | 系统定时刷新逾期状态：到达应还日期后未归还，状态自动标记为"逾期" |
| 前置条件 | 存在一条借阅记录的应还日期 = 昨天 |
| 预期结果 | 该借阅记录状态更新为"逾期"，读者账户标记有逾期未还 |
| 优先级 | P0 |

| 用例编号 | TC-BR-012 |
|----------|-----------|
| 测试点 | 逾期读者尝试借书 |
| 前置条件 | 读者账户存在逾期未还记录 |
| 预期结果 | 借阅被拒绝，提示"您有逾期图书未归还，请先处理" |
| 优先级 | P0 |

| 用例编号 | TC-BR-013 |
|----------|-----------|
| 测试点 | 逾期归还后清除逾期标记 |
| 前置条件 | 读者有一条逾期记录，归还该图书 |
| 预期结果 | 还书成功后该借阅记录标记为"已归还"，读者逾期状态清除，可正常借书 |
| 优先级 | P1 |

---

## 4. 查询统计模块

### 4.1 多条件检索

| 用例编号 | TC-QS-001 |
|----------|-----------|
| 测试点 | 按 ISBN 精确检索 |
| 前置条件 | 数据库存在该 ISBN 的图书 |
| 预期结果 | 返回匹配的图书记录 |
| 优先级 | P0 |

| 用例编号 | TC-QS-002 |
|----------|-----------|
| 测试点 | 按书名关键字检索 |
| 前置条件 | 数据库存在书名包含关键字的图书 |
| 预期结果 | 返回所有书名包含关键字的图书记录 |
| 优先级 | P0 |

| 用例编号 | TC-QS-003 |
|----------|-----------|
| 测试点 | 按"在库/已借出/已下架"状态筛选 |
| 前置条件 | 数据库存在不同状态的图书 |
| 预期结果 | 仅返回符合所选状态的图书 |
| 优先级 | P1 |

| 用例编号 | TC-QS-004 |
|----------|-----------|
| 测试点 | 多条件组合检索（分类 + 状态 + 关键字） |
| 前置条件 | 数据库有匹配全部条件的图书 |
| 预期结果 | 返回同时满足所有筛选条件的图书，分页正确 |
| 优先级 | P1 |

| 用例编号 | TC-QS-005 |
|----------|-----------|
| 测试点 | 空结果集（无图书匹配查询条件） |
| 前置条件 | 查询条件无匹配图书 |
| 预期结果 | 返回空列表，前端展示"未找到匹配的图书" |
| 优先级 | P2 |

### 4.2 借阅排行

| 用例编号 | TC-QS-006 |
|----------|-----------|
| 测试点 | 查看借阅次数排行 Top N |
| 前置条件 | 系统中存在多条借阅记录 |
| 预期结果 | 返回借阅次数排名前 N 的图书列表，按借阅次数降序排列 |
| 优先级 | P1 |

| 用例编号 | TC-QS-007 |
|----------|-----------|
| 测试点 | 按时间范围筛选排行（本月 / 本季度 / 本年） |
| 前置条件 | 系统中存在不同时间段的借阅记录 |
| 预期结果 | 仅统计选定时间范围内的借阅次数生成排行 |
| 优先级 | P2 |

| 用例编号 | TC-QS-008 |
|----------|-----------|
| 测试点 | 系统无借阅记录时查看排行 |
| 前置条件 | 系统无任何借阅记录（新建系统） |
| 预期结果 | 返回空列表，前端展示"暂无借阅数据" |
| 优先级 | P2 |

### 4.3 逾期统计

| 用例编号 | TC-QS-009 |
|----------|-----------|
| 测试点 | 查看当前逾期读者列表 |
| 前置条件 | 系统存在逾期未还的借阅记录 |
| 预期结果 | 返回所有逾期读者的信息及逾期图书列表，含逾期天数 |
| 优先级 | P1 |

| 用例编号 | TC-QS-010 |
|----------|-----------|
| 测试点 | 查看逾期图书统计（按分类 / 时间段） |
| 前置条件 | 系统存在逾期记录 |
| 预期结果 | 返回逾期图书的分类分布和时间分布统计数据 |
| 优先级 | P2 |

---

## 5. 系统管理模块

### 5.1 系统配置

| 用例编号 | TC-SM-001 |
|----------|-----------|
| 测试点 | 配置借阅上限（修改全局借阅上限数量） |
| 前置条件 | 以系统管理员登录 |
| 预期结果 | 配置保存成功，新借阅上限立即生效 |
| 优先级 | P1 |

| 用例编号 | TC-SM-002 |
|----------|-----------|
| 测试点 | 配置借阅期限（默认为 30 天，修改为 15 天） |
| 前置条件 | 以系统管理员登录 |
| 预期结果 | 配置保存成功，后续借阅的应还日期按新期限计算，已有借阅不受影响 |
| 优先级 | P1 |

| 用例编号 | TC-SM-003 |
|----------|-----------|
| 测试点 | 配置续借次数上限（默认 1 次，修改为 2 次） |
| 前置条件 | 以系统管理员登录 |
| 预期结果 | 配置保存成功，后续续借按新上限校验 |
| 优先级 | P2 |

| 用例编号 | TC-SM-004 |
|----------|-----------|
| 测试点 | 配置值为 0 或负数 |
| 前置条件 | 系统管理员登录 |
| 预期结果 | 前端/后端校验拦截，提示"配置值必须为正整数" |
| 优先级 | P2 |

### 5.2 操作日志

| 用例编号 | TC-SM-005 |
|----------|-----------|
| 测试点 | 查看操作日志列表 |
| 前置条件 | 以系统管理员登录，系统存在操作记录 |
| 预期结果 | 返回操作日志分页列表，含操作人、操作类型、操作时间、操作详情 |
| 优先级 | P1 |

| 用例编号 | TC-SM-006 |
|----------|-----------|
| 测试点 | 按时间范围筛选日志 |
| 前置条件 | 系统存在跨时间段的操作日志 |
| 预期结果 | 仅返回选定时间范围内的日志记录 |
| 优先级 | P2 |

| 用例编号 | TC-SM-007 |
|----------|-----------|
| 测试点 | 关键操作自动记录日志（注册、登录、借书、还书、修改配置） |
| 前置条件 | 依次执行上述关键操作 |
| 预期结果 | 每条操作在日志表中均有对应记录，操作人与操作内容正确 |
| 优先级 | P1 |

### 5.3 数据维护

| 用例编号 | TC-SM-008 |
|----------|-----------|
| 测试点 | 数据库备份 |
| 前置条件 | 以系统管理员登录，数据库正常运行 |
| 预期结果 | 系统生成数据库备份文件，前端提示备份成功 |
| 优先级 | P1 |

| 用例编号 | TC-SM-009 |
|----------|-----------|
| 测试点 | 备份文件列表查看 |
| 前置条件 | 系统存在历史备份文件 |
| 预期结果 | 返回备份文件列表，含文件名、创建时间、文件大小 |
| 优先级 | P2 |

| 用例编号 | TC-SM-010 |
|----------|-----------|
| 测试点 | 备份恢复 |
| 前置条件 | 以系统管理员登录，有可用备份文件 |
| 预期结果 | 系统数据恢复至备份时的状态，现有数据被覆盖 |
| 优先级 | P2 |

---

## 6. 跨模块集成测试

### 6.1 核心业务流程

| 用例编号 | TC-IT-001 |
|----------|-----------|
| 测试点 | 完整借还流程：管理员入库 → 编目 → 读者检索 → 借书 → 还书 |
| 前置条件 | 系统正常运行，各角色账户就绪 |
| 预期结果 | 全流程数据一致：入库后图书可检索 → 借书后状态为已借出 → 还书后状态为在库，借阅历史记录完整 |
| 优先级 | P0 |

| 用例编号 | TC-IT-002 |
|----------|-----------|
| 测试点 | 逾期闭环流程：借书 → 逾期 → 限制借阅 → 归还 → 解除限制 |
| 前置条件 | 借阅期限设置较短（如 1 天）以便触发逾期 |
| 预期结果 | 逾期后读者无法借书，归还后限制解除 |
| 优先级 | P1 |

### 6.2 并发场景

| 用例编号 | TC-IT-003 |
|----------|-----------|
| 测试点 | 多读者同时借阅同一本图书（仅剩 1 本库存） |
| 前置条件 | 目标图书库存 = 1，2 个读者同时发起借阅 |
| 预期结果 | 仅 1 个读者借阅成功，另一读者收到"该图书已被借出"提示 |
| 优先级 | P1 |

### 6.3 边界场景

| 用例编号 | TC-IT-004 |
|----------|-----------|
| 测试点 | 归还日期恰逢当天，当天还书不触发逾期 |
| 前置条件 | 应还日期 = 今天，读者在今天操作归还 |
| 预期结果 | 还书成功，不标记逾期 |
| 优先级 | P2 |

| 用例编号 | TC-IT-005 |
|----------|-----------|
| 测试点 | 空数据下的页面展示（新系统无图书、无读者、无借阅记录） |
| 前置条件 | 数据库为空 |
| 预期结果 | 各模块页面展示空状态占位提示，不报错、不崩溃 |
| 优先级 | P2 |

---

## 7. 测试策略

### 7.1 测试分层

| 层级 | 范围 | 工具 |
|------|------|------|
| 单元测试 | 后端 Service 层、工具函数 | Jest / Mocha |
| 单元测试 | 前端组件、Vuex Store | Vitest |
| 接口测试 | REST API 端点 | Supertest |
| 端到端测试 | 关键业务流程 | Cypress / Playwright |
| 手工测试 | UI/UX 验证、权限面验证 | 手工 |

### 7.2 测试环境

| 环境 | 用途 |
|------|------|
| 本地开发环境 | 开发自测 |
| 测试环境 | QA 完整测试 |
| 预发布环境 | 上线前验证 |

### 7.3 通过标准

- P0 用例 100% 通过
- P1 用例 100% 通过
- P2 用例通过率 >= 90%
- 无阻塞级（Blocker）缺陷
- 无严重级（Critical）缺陷

---

## 8. 测试用例统计

| 模块 | 用例数 | P0 | P1 | P2 |
|------|--------|----|----|-----|
| 用户管理 | 16 | 5 | 8 | 3 |
| 图书管理 | 14 | 5 | 5 | 4 |
| 借阅管理 | 13 | 6 | 5 | 2 |
| 查询统计 | 10 | 2 | 4 | 4 |
| 系统管理 | 10 | 0 | 5 | 5 |
| 集成测试 | 5 | 1 | 3 | 1 |
| **合计** | **68** | **19** | **30** | **19** |
